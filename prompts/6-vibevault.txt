Use subagents for each task below to create an Unturned RocketMod Vault plugin:

1. Look up what vault means in the context of Unturned. Its related to a virtual storage thats like enderchests in Minecraft.

2. Create a plugin called vibevault. Analyze the Unturned and RocketMod codes in references to understand how to create a plugin that adds a vault system to Unturned using RocketMod.

3. Add related permission checks.

4. Add configurable vault sizes that are accessible based on permissions.

5. Add a /vault command that opens the biggest vault the player has access to by default, and also allows specifying a vault number to open specific vaults.

6. Implement a system to save the vault contents persistently, configurable by eitehr using MySQL or directly with raw file storage. 

7. Ensure that the vault contents are secure and only accessible by the player who owns them, unless they have specific permissions to access others' vaults.

8. Add group vaults that are shared among players in the same game group. Only one player can access the group vault at a time, and it should have its own permission checks to control who can access it.

9. Add a command for a readonly-view of the vault contents, allowing players with specific permissions to view but not modify the contents of a vault. Also add a command to list all vaults a player has access to, including group vaults.

10. Implement a system to handle vault upgrades, allowing players to increase their vault size or access more vaults based on certain criteria, such as in-game achievements or purchases.

11. Add a feature to allow players to share specific vaults with other players, granting them access to view or modify the contents based on permissions.

12. Ensure that the plugin is optimized for performance, especially when handling large vaults or multiple players accessing their vaults simultaneously.

13. Provide clear documentation for the plugin, including installation instructions, configuration options, and usage guidelines for both players and server administrators inside a README.md.

14. Add a public API for other developers to interact with the vault system, allowing them to create their own plugins that can integrate with the vault.

15. Add a /trash command that allows players to delete items from their inventory by dragging and dropping them into a virtual trash bin. This command should also have permission checks to prevent abuse.

16. Add tests to ensure the functionality and stability of the vault plugin. This should include unit tests for individual components, as well as integration tests to ensure that the plugin works correctly within the Unturned and RocketMod environment.